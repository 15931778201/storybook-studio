完全理解你的考量。创作者需要的是**高效、专业、逻辑清晰**的生产工具，后台管理员需要的是**精准、可控、操作明确**的管理系统。过度童话化的包装反而会干扰注意力。

我们保留阅读器面向儿童的温暖设计，但将**编辑器和后台**彻底“去童话化”，回归专业工具的本质。

以下是修订后的完整产品线设计，采用精准、专业的表述。

---

## 产品线全景（务实版）

| 产品模块           | 用户                       | 定位                         | 使用终端                           |
| ------------------ | -------------------------- | ---------------------------- | ---------------------------------- |
| **AI绘本编辑器**   | 创作者（家长/老师/插画师） | 专业内容生产工具，AI辅助提效 | PC Web（主力）+ 小程序（素材采集） |
| **儿童绘本阅读器** | 儿童（3-10岁）及家长       | 沉浸式交互阅读体验           | UniApp（iOS/Android/H5/小程序）    |
| **管理员后台**     | 运营/审核人员              | 内容审核、用户管理、数据监控 | PC Web                             |
| **后端服务**       | 系统                       | 支撑全业务的数据与AI服务     | Node.js + Express                  |

---

## 一、AI儿童绘本编辑器（专业生产工具）

### 1.1 产品定位
- **核心用户**：非技术背景的内容创作者，包括家长、教师、童书作者、插画师。
- **核心价值**：降低交互式有声绘本的制作门槛，通过**结构化编辑**与**AI辅助生成**，将创作周期从天级压缩至小时级。
- **设计原则**：功能可见性优先，操作路径最短，避免过度隐喻。

### 1.2 信息架构

```
工作台首页
├── 项目管理：创建新项目 / 草稿箱 / 已发布作品
├── 素材库：我的图片 / 我的音频 / 公共素材库
└── 账户设置：个人资料 / 创作数据看板
```

### 1.3 功能模块详解

#### 模块 1：项目管理

**（1）项目列表页**
- 卡片式布局展示所有项目。
- 每个项目卡片包含：封面缩略图、标题、最后编辑时间、状态标签（草稿/审核中/已发布/审核驳回）。
- 支持按标题搜索、按状态筛选、按时间排序。
- 悬浮操作：编辑、复制、删除、查看数据（已发布作品）。

**（2）新建项目**
- 三种创建方式：
  - **空白项目**：从零开始创建，需手动设置书名、年龄段等元数据。
  - **AI 生成大纲**：输入故事主题或关键词（如“小恐龙学分享”），AI 生成 6-12 页的分页大纲及每页文字初稿。
  - **导入模板**：从系统模板库选择预设结构（如“习惯养成类”、“科普认知类”）。

#### 模块 2：页面编辑器（核心工作区）

**界面布局（三栏结构）**

| 区域           | 宽度占比 | 内容                                                         |
| -------------- | -------- | ------------------------------------------------------------ |
| **左侧面板**   | 20%      | 页面缩略图导航栏（支持拖拽排序、新增/删除/复制页面）         |
| **中央画布**   | 55%      | 所见即所得的编辑区，默认为竖屏手机比例（375x667），可切换横屏 |
| **右侧属性栏** | 25%      | 根据当前选中元素动态显示配置项，分三个标签页：**样式**、**交互**、**动画** |

**（1）页面管理**
- 页面列表以缩略图形式纵向排列，显示页码。
- 支持：添加页面、复制页面、删除页面、拖拽调整顺序。
- 双击缩略图可快速设置页面背景（纯色/图片）。

**（2）元素操作**
- 支持的元素类型：**文本**、**图片**、**音频热区**、**按钮**。
- **添加方式**：
  - 从左侧素材库拖拽至画布。
  - 点击工具栏对应图标插入。
- **画布操作**：
  - 选中元素后显示控制框，支持拖拽移动、缩放、旋转。
  - 支持多选（Shift+点击）进行对齐、分布操作。
  - 提供吸附对齐线、标尺辅助定位。

**（3）右侧属性栏——样式标签页**

根据元素类型显示对应样式配置：

- **通用样式**：位置（X/Y坐标）、尺寸（宽/高）、旋转角度、透明度、层级顺序。
- **文本元素专属**：字体、字号、颜色、对齐方式、行距、字间距。
- **图片元素专属**：裁剪、圆角、边框、阴影、滤镜（亮度/对比度/模糊）。
- **背景设置**：背景色、背景图片、填充方式。

**（4）右侧属性栏——交互标签页**

为元素配置点击/触摸事件响应。

- **触发器**：单击、双击、长按（可多选）。
- **响应动作**（可叠加多个）：
  - **播放音频**：从素材库选择或现场录制，支持音量、是否允许重叠播放。
  - **运行动画**：选择该元素已配置的动画（见动画标签页）。
  - **跳转页面**：跳转到指定页码。
  - **显示/隐藏元素**：控制其他元素的可见性。
  - **执行脚本**（高级）：自定义 JavaScript 逻辑。
- **交互预览**：点击“测试”按钮，在画布上模拟触发效果。

**（5）右侧属性栏——动画标签页**

为元素添加入场/强调/退场动画效果。

- **动画模式**：
  - **预设动画**：选择内置模板（淡入、滑入、弹跳、缩放、旋转等）。
  - **关键帧编辑**（高级）：基于时间轴进行精细控制。
- **动画参数**：持续时间、延迟、重复次数、缓动函数。
- **触发方式**：页面加载时自动播放 / 由交互事件触发。
- **动画列表**：一个元素可配置多个动画，按顺序或并行执行。

**（6）页面级设置**
- 页面背景音乐：进入该页时自动播放的环境音或 BGM。
- 页面旁白：支持上传真人配音或使用 AI 语音合成（可选择声线、语速）。

#### 模块 3：素材库

**（1）我的素材**
- 分类展示：全部、图片、音频。
- 视图切换：网格视图 / 列表视图。
- 上传功能：
  - 支持批量上传图片（JPEG/PNG/GIF/WebP）。
  - 支持上传音频（MP3/M4A/WAV），限制单文件 ≤ 20MB。
- 素材操作：重命名、添加标签、删除、查看引用次数。
- **AI 辅助功能**：
  - **智能抠图**：选中图片后一键去除背景，生成透明 PNG。
  - **风格迁移**：将照片转换为绘本风格（水彩/彩铅/扁平卡通）。

**（2）公共素材库**
- 系统提供的免费可商用素材，按分类（场景/角色/道具/音效）浏览。
- 支持关键词搜索。
- 点击素材即可添加至“我的素材”。

**（3）小程序端素材采集（配套）**
- 独立小程序，仅提供三个核心功能：
  1. **拍照上传**：调用相机拍照，自动压缩并上传至云端素材库，可选是否自动抠图。
  2. **录音上传**：长按录音，松开即传，自动存入音频素材库。
  3. **草稿预览**：同步 PC 端项目，在手机上查看当前编辑效果。

#### 模块 4：AI 辅助生成

作为工具栏中的独立面板，提供以下能力：

| 功能             | 输入                 | 输出                           | 应用场景                   |
| ---------------- | -------------------- | ------------------------------ | -------------------------- |
| **故事大纲生成** | 主题/关键词/故事梗概 | 分页大纲 + 每页文字初稿        | 快速搭建绘本骨架           |
| **单页文案优化** | 当前页面文字         | 改写/扩写/缩写版本             | 润色文本表达               |
| **图文匹配建议** | 页面现有图片 + 文字  | 推荐风格一致的背景图或装饰元素 | 加快页面布局               |
| **动画脚本建议** | 页面文字描述         | 推荐的动画类型及元素列表       | 为静态页面提供动态设计思路 |

- 所有 AI 生成结果均为**建议**，用户可采纳、修改或放弃。
- 支持历史记录回溯，可撤销 AI 的修改。

#### 模块 5：发布与版本管理

**（1）预览**
- 点击“预览”生成二维码，手机扫码可在阅读器中查看实时效果。
- 预览模式下交互事件、动画、音频均正常工作。

**（2）提交审核**
- 填写发布信息：
  - 封面图（自动截取首页或手动上传）。
  - 标题、副标题（选填）。
  - 适读年龄范围（多选）。
  - 内容简介（面向家长）。
  - 分类标签（最多3个）。
- **AI 内容预检**（自动触发）：
  - 敏感词检测（文字、音频转文字检测）。
  - 图片质量评估（分辨率、清晰度）。
  - 版权风险提示（检测到疑似网络图片）。
- 勾选原创声明，提交进入人工审核队列。

**（3）版本管理**
- 每次提交审核时自动保存一个快照。
- 审核通过后，发布版本不可修改。如需更新，需创建新版本并再次提交审核。
- 支持回滚至历史版本（仅草稿状态）。

---

## 二、儿童绘本阅读器

### 2.1 进入与身份

- **启动页**：月光森林，书页纷飞。
- **读者角色**：孩子可以创建自己的“小读者名片”（选一个动物头像，取个昵称），家长可绑定主账号进行时长管理和内容过滤。

### 2.2 图书馆大厅（首页）

- **视觉**：一座圆形大厅，四周是巨大的弧形书架，书脊朝外，微微发光。中央有旋转木马展示**推荐绘本**。
- **发现绘本**：
  - **魔法罗盘（搜索/筛选）**：点击罗盘，弹出气泡选项：🐻动物、👸公主、🚀太空、🦕恐龙……以及“妈妈讲过的”（历史记录）。
  - **AI 小馆员**：一只小獾，偶尔会跑过来推荐：“你上次喜欢小狐狸的故事，这本讲小狐狸找妈妈的，要试试吗？”

### 2.3 阅读体验（极致沉浸）

- **翻页**：真实物理翻页效果，带沙沙声。
- **自动播放模式**：家长可以开启“睡前连续播放”，读完一本自动推荐同类绘本并开始播放，屏幕亮度逐渐变暖。
- **互动反馈**：
  - 点击任何能动的地方，都会有即时、丰富但不夸张的反馈（音效+微动效）。
  - 孩子如果在一个地方反复点击，系统会认为他特别喜欢，可能会触发一个隐藏小彩蛋（比如角色对他眨眨眼）。
- **护眼与家长控制**：
  - 从屏幕底部上滑呼出**家长中心**（需简单算术验证，防止孩子误入）。
  - 可设置：使用时长、休息间隔、禁止时段、阅读报告。

### 2.4 个人小屋（我的）

- **我的宝藏盒**：收藏的绘本和每读完一本获得的“故事星星”。
- **我的伙伴**：孩子可以选择一个虚拟伙伴（小狐狸、小熊），它会根据孩子的阅读量变化心情和对话。
- **阅读护照**：每读完一本书，护照上多一个签证章，记录阅读日期和本书最有趣的角色。

### 2.5 社交与分享（谨慎设计）

- **无公开评论区**，避免负面评价。
- **只能“送出一朵花”**（点赞）或“送给好朋友”（生成带精美封面的分享卡片，分享到微信）。
- **家庭共享**：一个家庭账号下，大宝的书架可以分享给二宝。

---

## 三、管理员后台（内部运营系统）

### 3.1 设计原则
- **高效操作**：批量处理能力、快捷键支持、数据导出。
- **信息明确**：所有状态、指标清晰展示，无歧义。
- **权限分级**：区分超级管理员、内容审核员、运营编辑等角色。

### 3.2 功能模块

#### 模块 1：仪表盘
- 核心数据卡片：累计用户数、今日新增、待审核绘本数、本周上架数。
- 趋势图表：近7日新增用户/绘本/阅读量曲线。
- 快速入口：待审核列表、热门绘本排行。

#### 模块 2：内容审核

**（1）待审核列表**
- 列表字段：封面、标题、作者、提交时间、AI 预检结果（通过/风险/警告）。
- 支持按提交时间排序、按风险等级筛选。
- 点击进入审核详情页。

**（2）审核详情页**
- **预览区**：嵌入阅读器组件，可完整翻阅、点击交互，体验与用户端一致。
- **信息区**：展示绘本元数据、作者信息、AI 预检报告（包含敏感词命中位置、低质图片页码）。
- **审核操作**：
  - **通过**：绘本状态变更为“已发布”，同步上架至阅读器，并推送通知给作者。
  - **驳回**：必填驳回理由（提供常用驳回话术模板，如“图片版权存疑”、“文字含敏感信息”），驳回后作者可在编辑器查看原因并修改重提。
  - **转审**：将任务转给其他审核员。
- **批量操作**：列表页支持批量通过/驳回（适用于低风险作品）。

**（3）已审核记录**
- 查询所有历史审核记录，支持按审核员、时间范围、结果状态筛选。

#### 模块 3：内容管理

**（1）绘本管理**
- 展示所有已上架绘本，支持搜索（标题/作者）、分类筛选。
- 操作：下架（即时生效）、推荐至首页、设置精选标签、查看详情。

**（2）评论/反馈管理**
- 若阅读器设有反馈渠道（如举报），此处集中处理用户举报，可屏蔽内容或回复用户。

**（3）公共素材库管理**
- 上传、分类、标签化维护公共素材。
- 可查看素材引用次数，删除低质或侵权素材。

#### 模块 4：用户管理

- **用户列表**：按用户ID、手机号、注册时间查询。
- **用户详情**：基本信息、注册时间、作品列表（创作者）、阅读记录（读者）。
- **账号状态**：正常 / 禁用。可对违规用户进行封禁处理。
- **创作者认证**：支持人工标记“认证创作者”，给予更高曝光权重。

#### 模块 5：数据统计

- **内容数据**：绘本阅读量、完读率、平均阅读时长、互动热力图（展示每页点击分布）。
- **用户数据**：新增留存、活跃时段分布、设备类型比例。
- **创作者数据**：作品产量、被阅读量、互动数据排行。
- **导出功能**：所有报表支持 CSV/Excel 导出。

#### 模块 6：系统设置

- 审核阈值配置：AI 预检风险等级的自动处理策略。
- 推荐位管理：配置首页轮播图、精选绘本池。
- 运营通知模板：编辑审核结果推送文案。
- 角色与权限管理：创建/编辑后台账号及对应权限。

---

## 四、后端服务（支撑层）

描述风格调整为技术文档式，保留核心设计。

### 4.1 技术栈
- **运行环境**：Node.js 18+，Express 框架，TypeScript 编写。
- **数据库**：MongoDB（主存储），Redis（缓存与队列），MinIO（对象存储，可替换为阿里云OSS）。
- **AI 服务**：Ollama 本地部署，加载 Gemma 4 或 Qwen3-VL 模型，提供 HTTP API 供后端调用。

### 4.2 服务模块

| 模块         | 职责                                                         | 主要接口                        |
| ------------ | ------------------------------------------------------------ | ------------------------------- |
| **用户服务** | 注册/登录、JWT签发、权限校验、家庭账号关联                   | `/api/auth/*`, `/api/user/*`    |
| **绘本服务** | 项目、页面、元素的 CRUD，版本控制                            | `/api/project/*`, `/api/page/*` |
| **素材服务** | 文件上传、存储、缩略图生成、AI 标签提取                      | `/api/asset/*`                  |
| **AI 网关**  | 统一封装对 Ollama 的调用，异步任务管理（Bull队列），进度推送（WebSocket） | `/api/ai/*`                     |
| **审核服务** | 审核流程状态机、AI预检结果存储、审核记录                     | `/api/review/*`                 |
| **阅读服务** | 记录阅读历史、互动事件埋点、收藏                             | `/api/reading/*`                |
| **数据统计** | 定时聚合阅读数据、生成报表                                   | `/api/analytics/*`              |
| **消息服务** | 推送审核结果、系统通知（小程序订阅消息/邮件）                | 内部调用                        |

### 4.3 AI 服务异步处理机制
- 耗时任务（如故事生成、图片风格迁移）提交至 Bull 队列。
- 队列 Worker 进程调用 Ollama API 进行处理。
- 前端通过 WebSocket 或轮询获取任务进度与结果。

### 4.4 数据库核心集合设计（精简描述）

- `users`：用户账号与资料。
- `projects`：绘本项目，包含元数据、状态、版本信息。
- `pages`：页面数据，嵌套存储元素数组（包含样式、交互、动画配置）。
- `assets`：素材元数据与存储路径。
- `reviews`：审核记录。
- `reading_logs`：阅读行为流水。

